- name: Configure Traefik
  hosts: control_plane
  become: true

  tasks:
    - name: Apply Traefik HelmChartConfig
      ansible.builtin.command:
        cmd: kubectl apply -f "{{ playbook_dir }}/../kubernetes/infrastructure/traefik/helmchartconfig.yaml"